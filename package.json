{
  "private": true,
  "scripts": {
    "bits:install": "yarn --prod && npm run browserify",
    "lint": "eslint --fix *.js server *js test",
    "browserify": "browserify app/elements/network-manager/scripts/dev-utils.js -o app/elements/network-manager/scripts/bundle.js",
    "test": "mocha --timeout 5000 test/*.js test/**/*.js",
    "test-specific": "mocha",
    "build": "yarn && npm run lint && npm run browserify && npm run test"
  },
  "dependencies": {
    "debug": "^2.2.0",
    "ip": "^1.1.4",
    "oui": "^4.4.18",
    "udev": "^0.4.0"
  },
  "devDependencies": {
    "browserify": "^14.0.0",
    "chai": "^3.5.0",
    "chai-as-promised": "^6.0.0",
    "eslint": "^3.10.2",
    "eslint-config-google": "^0.6.0",
    "mocha": "^3.1.2",
    "mockery": "^2.0.0",
    "nodemon": "^1.11.0",
    "removeNPMAbsolutePaths": "^0.0.3"
  }
}
